{
  "name": "community-test",
  "version": "1.0.0",
  "description": "Community Test: Next.js + LINE bot webhook, plain HTML status, rule-based command, file-based status, ready for local & Vercel",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "next": "^16.1.6"
  }
}